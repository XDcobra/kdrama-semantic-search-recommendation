- **`raw/`** — downloaded K-drama source files (gitignored by default; see root `.gitignore`).
- **`processed/`** — cleaned tabular/JSON ready for embedding and import (gitignored; keep a small sample only if you commit examples).

Do not commit large proprietary datasets; document provenance in the lab report.
