**Docker Compose** for local stack: **Weaviate** + (later) **Go API** + optional one-shot embedder.  
Start with `docker compose up` from this directory or project root when `docker-compose.yml` is added.
