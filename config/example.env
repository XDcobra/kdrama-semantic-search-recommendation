# Local development (copy to .env in repo root; do not commit secrets)
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_SCHEME=http

# Must match the model used to build drama vectors and query vectors
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# API
HTTP_ADDR=:8080
