Configuration templates: **Weaviate** URL, class schema hints, **embedding model** name (must match between batch index and live query in Go/Python).  
Copy `example.env` to repo root `.env` (gitignored) for local development.
